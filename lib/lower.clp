; lower from lisp structures to a simpler format

(defmodule lower
           (import cortex
                   ?ALL)
           (import lisp-parse
                   ?ALL)
           (export ?ALL))

