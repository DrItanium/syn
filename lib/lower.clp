; lower from lisp structures to a simpler format

(defmodule lisp->intermediary
           (import cortex
                   ?ALL)
           (import lisp-parse
                   ?ALL)
           (export ?ALL))

